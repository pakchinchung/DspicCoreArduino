name=EEPROM
version=1.0.0
author=dspicArduino
maintainer=dspicArduino
sentence=Flash-emulated EEPROM for dsPIC33CK.
paragraph=Emulates EEPROM in a reserved Flash page, shadowed in RAM. Reads are from the buffer; call EEPROM.commit() to persist writes to Flash (data survive a reset). Same API as the standard Arduino EEPROM library (read/write/update/get/put/length/operator[]).
category=Data Storage
url=
architectures=dspic
